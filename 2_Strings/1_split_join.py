def split_and_join(line):
    # write your code here
    temp = line.split()
    return "-".join(temp)
    # this should be easy, so no detailed breakdown
    # if lost, refer to last two days folder: 0_into and 1_basics

if __name__ == '__main__':
